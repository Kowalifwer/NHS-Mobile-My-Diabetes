image:
    node:latest

stages:
    - installation
    - build

installing-dependencies:
    stage: installation
    tags: 
        - docker
    script:
        - npm install
        - echo "Managed to install correctly"

build-job:
    stage: build
    tags: 
        - docker
    script:
        - npm install
        - cd nhs_mobile
        - expo start
        - echo "Built project successfully"
